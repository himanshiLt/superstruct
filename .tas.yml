framework: mocha
tier: xlarge
postMerge:
  pattern:
    - "./test/api/*.ts"
    - "./test/validation/**/*.ts"
preMerge:
  pattern:
    - "./test/api/*.ts"
    - "./test/validation/**/*.ts"
preRun:
     command:
        - yarn install
        - yarn build:cjs
postRun:
     command:
        - yarn --version
version: 2.1
